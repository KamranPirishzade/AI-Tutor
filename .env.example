# Get a key from Google AI Studio: https://aistudio.google.com/apikey
# Free tier has very low daily request caps (as low as 10/day for TTS) —
# see project memory / recent chat history if you hit 429 RESOURCE_EXHAUSTED.
GEMINI_API_KEY=
